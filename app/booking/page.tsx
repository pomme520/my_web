'use client';
import {useState} from 'react';

export default function Booking(){const [sent,setSent]=useState('');const [loading,setLoading]=useState(false);const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();setLoading(true);const form=new FormData(e.currentTarget);const res=await fetch('/api/repair-orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(form.entries()))});const data=await res.json();setLoading(false);if(res.ok){setSent(data.orderNumber);e.currentTarget.reset();}};return <div className="mx-auto max-w-3xl px-6 py-16"><h1 className="text-4xl font-black">線上維修預約</h1><p className="mt-4 text-slate-600">這是測試版，送出後會產生暫存案件編號。</p><form onSubmit={submit} className="mt-8 space-y-5 rounded-2xl border bg-white p-6 shadow-sm"><div className="grid gap-5 md:grid-cols-2"><label>姓名<input required name="customerName" className="mt-2 w-full rounded-xl border p-3" /></label><label>電話<input required name="phone" className="mt-2 w-full rounded-xl border p-3" /></label><label className="md:col-span-2">Email<input required type="email" name="email" className="mt-2 w-full rounded-xl border p-3" /></label><label>設備類型<select name="deviceType" className="mt-2 w-full rounded-xl border p-3"><option>桌上型電腦</option><option>筆記型電腦</option><option>其他設備</option></select></label><label>問題類型<select name="issueType" className="mt-2 w-full rounded-xl border p-3"><option>無法開機</option><option>系統錯誤</option><option>速度變慢</option><option>硬體升級</option><option>其他</option></select></label></div><label>問題描述<textarea required name="description" className="mt-2 min-h-32 w-full rounded-xl border p-3" placeholder="請描述遇到的問題" /></label><button disabled={loading} className="rounded-full bg-brand px-6 py-3 font-bold text-white disabled:opacity-50">{loading?'送出中…':'送出預約'}</button>{sent&&<div className="rounded-xl bg-green-50 p-4 text-green-700">預約成功！案件編號：<strong>{sent}</strong></div>}</form></div>}
